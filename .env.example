# Echo 环境变量示例
# 复制为 .env.local 后按需填写（.env.local 不会提交到 git）

# ===== 前端变量（VITE_ 前缀，会暴露给客户端 bundle）=====

# 后端 API 基础地址：
#   mock 模式下不会调用，随意；
#   真实模式（本地 vercel dev 或 Vercel 生产）用 /api（同源，免跨域）
VITE_API_BASE_URL=/api

# 是否使用 mock 数据（true: 走前端 mock；false: 走真实后端）
# 后端未就绪前保持 true；后端配好后改 false
VITE_USE_MOCK=true

# ===== 后端变量（不带 VITE_ 前缀，只在 /api 服务端函数中可用，不会进前端）=====

# DeepSeek API Key —— 在 https://platform.deepseek.com 申请
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx

# Supabase 项目地址 —— 在 https://supabase.com 建项目后获取
SUPABASE_URL=https://xxxxxxxx.supabase.co

# Supabase service_role key（服务端用，绕过 RLS）—— 在 Supabase Settings → API
SUPABASE_SERVICE_ROLE=eyJhbGciOiJIUzI1NiIs...
